
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: planners.apps.hse.ru
spec:
  group: apps.hse.ru
  names:
    kind: Planner
    listKind: PlannerList
    plural: planners
    singular: planner
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Planner is the Schema for the planners API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PlannerSpec defines the desired state of Planner
            properties:
              active:
                type: boolean
              constraints:
                properties:
                  resource_range:
                    properties:
                      maxCpu:
                        format: int64
                        maximum: 100
                        minimum: 1
                        type: integer
                      maxMemory:
                        format: int64
                        maximum: 100
                        minimum: 1
                        type: integer
                      minCpu:
                        format: int64
                        maximum: 100
                        minimum: 1
                        type: integer
                      minMemory:
                        format: int64
                        maximum: 100
                        minimum: 1
                        type: integer
                    type: object
                type: object
              delay:
                minimum: 1
                type: integer
              namespaces:
                items:
                  type: string
                type: array
            required:
            - active
            type: object
          status:
            description: PlannerStatus defines the observed state of Planner
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
